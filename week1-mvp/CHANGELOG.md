# 更新日志

## 2026-05-25 14:09 +08:00

### 修改

- `产品上架` 页 `媒体 Media` 图片角色下拉改为 `主图 / 背面 / 细节` 顺序。
- 用户通过下拉或星标设置角色后，图片会按 `主图 → 背面 → 细节` 自动稳定排列；同一角色内保持原有相对顺序。
- `主图` 和 `背面` 保持单张语义，重新选择时会将旧的同类图片降为 `细节`。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 13:57 +08:00

### 修改

- 侧边栏 `HEX 换色`、`批量摄影`、`服饰场景图`、`产品上架` 入口关闭自动预取；当前页重复点击会被拦截，避免触发页面自动刷新。
- `HEX 精准换色`、`批量摄影`、`服饰场景图`、`产品上架` 页面标题右侧新增刷新图标按钮，点击后手动刷新当前页面。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/recolor`、`/batch-photo`、`/scene-tools`、`/product-listing` 页面请求返回正常。

## 2026-05-25 13:43 +08:00

### 修改

- `产品上架` 页 `变体、价格与库存` 区将单一 `价格` 字段拆分为 `原价` 和 `售价`；售价继续作为必填价格字段。
- 产品上架 AI 提示词和解析映射新增 `原价 / 售价` 字段；旧输出中的 `价格` 仍会兼容填入售价，已保存的默认预设会自动刷新为新版提示词。
- 同步到 Shopify 时，`售价` 写入 Variant `price`，`原价` 写入 Variant `compareAtPrice`。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 13:35 +08:00

### 修改

- 生成完成后的图片卡片新增悬停竖向工具栏，提供 `裁剪` 和 `下载` 操作；原有单张下载能力保留。
- 生成结果图片支持双击打开原图预览；裁剪确认后会导出裁剪后的图片文件，不覆盖任务中的原图。
- 结果网格中的手动调色入口迁移到同一套悬停竖向工具栏，保持生成后图片操作风格统一。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/recolor` 与 `http://localhost:3000/history` 页面请求返回正常。

## 2026-05-25 12:18 +08:00

### 修改

- `产品上架` 页 `开始解析` 强制要求 `媒体 Media` 中存在可读取的商品图片；没有图片或图片无法读取时会直接提示，不再只按文字提示词生成。
- GPT 解析提示词加强为必须以商品图片为主要依据，并在解析成功状态中显示 `已传入 N 张图`。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 12:12 +08:00

### 修改

- `产品上架` 页 `开始解析` 的 GPT 默认模型从 `gpt-4o-mini` 改为 `gpt-5.4`，避免中转站默认分组无该模型渠道时调用失败。
- 新增 `gpt_text_model` 默认配置项，默认值为 `gpt-5.4`，产品上架文字解析会优先读取该配置。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 12:09 +08:00

### 修改

- `系统设置` 中 `Gpt API Key` 的 URL 配置改为 `中转站 URL（可选）`，语义为 OpenAI 兼容接口的 `baseURL`，不是本地网络代理。
- 新增 `gpt_base_url` 配置项；产品上架文字解析调用 GPT 时会把该地址传给 OpenAI SDK 的 `baseURL`。
- 保留旧 `gpt_proxy_url` 兼容读取，已填写旧字段时仍可作为中转站 URL 使用；新保存会写入 `gpt_base_url`。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/admin/settings` 与 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 12:00 +08:00

### 修复

- 修复 `产品上架` 页 `开始解析` 失败后 `完整大模型输出` 保持空白的问题；现在解析失败会直接在输出框显示错误原因。
- `gpt_proxy_url` 支持自动补全协议，填写 `127.0.0.1:7892` 会按 `http://127.0.0.1:7892` 使用；代理不可用时会返回明确错误，不再静默回退到无代理调用。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 11:52 +08:00

### 修改

- `产品上架` 页左侧 `开始解析` 的大模型调用改为读取系统设置中的 `gpt_api_key`，不再走 Gemini 视觉模型配置。
- 产品上架文字解析接口会同时读取 `gpt_proxy_url`，在配置代理时通过代理调用 GPT；Media 中的本地图片会以视觉输入传给 GPT。
- 解析使用独立的 GPT 调用记录，缺少 `Gpt API Key` 时会提示去系统设置填写。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 11:43 +08:00

### 修改

- `系统设置` 的 `Gpt API Key` 配置卡片新增 `代理 URL（可选）` 输入框，保存逻辑与 `OpenAI API Key（gpt-image-2）` 的代理配置一致。
- 新增 `gpt_proxy_url` 全局配置项，支持 `http://` / `https://` URL 校验、保存和回显。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/admin/settings` 页面请求返回正常。

## 2026-05-25 11:39 +08:00

### 修改

- `系统设置` 新增 `Gpt API Key` 配置卡片，展示格式、输入框、清空按钮和保存按钮与 `OpenAI API Key（gpt-image-2）` 保持一致。
- 新增 `gpt_api_key` 全局配置项，支持 `sk-...` 格式校验、敏感字段脱敏显示和保存/清空。
- 通用设置接口和后台设置接口已将 `gpt_api_key` 视为敏感配置，避免明文暴露或误写入脱敏值。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/admin/settings` 页面请求返回正常。

## 2026-05-25 11:27 +08:00

### 修改

- `产品上架` 页预设提示词下拉改为自绘可滚动列表，支持在下拉列表中右键删除某条预设。
- 预设提示词存储改为完整快照，删除系统默认预设后刷新页面也会保持删除状态。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 11:23 +08:00

### 修复

- 调整 `产品上架` 页 `保存为预设` 行为：每次保存都会新增一条预设提示词，不再覆盖当前选中预设或同内容预设；旧预设会继续保留在下拉框中供滚动选择。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 11:20 +08:00

### 修复

- 修复 `产品上架` 页预设提示词保存名称不生效的问题：当提示词内容已存在时，现在会更新该预设名称并同步到下拉框，而不是直接判定重复。
- 编辑提示词内容时不再自动清空当前预设名称，避免保存时丢失用户输入的名称。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 11:16 +08:00

### 修改

- `产品上架` 页预设提示词新增 `预设名称` 输入框和 `重命名` 按钮；选中预设后可直接修改名称并保存。
- 系统默认预设被重命名时，会保存为本地覆盖名称，刷新页面后仍保留用户自定义名称。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 11:10 +08:00

### 修改

- `产品上架` 页左侧新增 `大模型提示词` 输入框、`预设提示词` 下拉和 `保存为预设` 功能；用户从历史记录发送图片到产品上架后，需要手动输入或选择提示词再解析。
- 新增 `开始解析` 流程：将当前 `媒体 Media` 图片和用户提示词提交给大模型，返回内容写入 `完整大模型输出`，并同步生成清理后预览。
- 新增产品上架文字解析接口 `/api/product-listing/ai-output`，复用视觉理解模型读取本地 `/assets/...` 媒体图片，输出 Shopify 商品字段文本。
- 大模型输出清理逻辑继续去除多余空格、乱码替换符和无效控制字符；点击 `清理文字` 或 `填入表单` 时会使用清理后的内容。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-25 10:42 +08:00

### 修改

- `产品上架` 页 `媒体 Media` 区右下角新增 `本地上传` 按钮，支持一次选择多张本地图片并加入当前商品 Media 草稿。
- 新增产品上架 Media 本地上传接口，上传图片会保存到本地 `/assets/uploads/product-media`，并自动进入可预览、排序、标记角色的 Media 列表。

### 验证

- `npx.cmd tsc --noEmit` 通过。

## 2026-05-25 10:28 +08:00

### 修改

- `批量摄影` 生成参数新增 `1K 清晰（推荐）` 清晰度，并设为默认；原 `2K 高清（推荐）` 调整为 `2K 高清`，生成请求会按当前选中的清晰度传给后端。
- `批量摄影` 上传产品图区拆分网页与本地来源：本地上传不再映射到 `已选`，`已选` 只展示网页选择图片；网页或本地任一来源仍可作为后续生图输入。
- `服饰场景图` 右侧生成栏默认画质改为 `1K（最便宜）`，后端默认兜底同步改为 `1K`。
- `服饰场景图` 上传产品图区同步拆分网页与本地来源：本地上传只显示在 `本地`，不再进入 `已选`。
- `服饰场景图` 比例下拉新增 `2:3 竖`，提交时通过 `aspect_ratio` 传给生成接口，让模型按所选比例合成图片。
- `历史记录` 每条任务记录在花费左侧新增 `产品上架` 按钮；未绑定 Shopify 时点击跳转到 `产品上架` 并提示 `请输入 Shopify 密钥`，已绑定时将该任务成功图片加入 `产品上架` 页的 `媒体 Media`。
- 生成任务完成页新增 `产品上架` 入口，可将当前完成图或选中图加入 `产品上架` 页的 `媒体 Media`。
- `产品上架` 页的 `媒体 Media` 区由占位卡片升级为可编辑图片区，支持预览、删除、上移、下移、设为主图，以及标记 `主图 / 细节 / 背面`。
- `同步到 Shopify` 时会携带 `媒体 Media` 图片；本地 `/assets/...` 生成图会先通过 Shopify staged upload 暂存上传，再随商品创建写入 Shopify Media。
- 新增产品上架 Media 草稿工具，用浏览器本地存储暂存历史记录/生成结果加入的图片，避免提前同步到 Shopify 官方后台。

### 验证

- `npx.cmd tsc --noEmit` 通过。
- 本地 `http://localhost:3000/history` 与 `http://localhost:3000/product-listing` 页面请求返回正常。

## 2026-05-23

### 修改

- 通用模型文字解析提示词改为强约束 JSON 输出，并新增中文 `key: value` 文本兜底解析；当中转模型返回“服装结构化属性分析...”这类说明文字时，会尽量转换为项目需要的款式属性结构。
- 侧边栏新增 `产品上架` 入口，位置在 `服饰场景图` 与 `历史记录` 之间。
- 新增 `产品上架` 页面布局：未绑定 Shopify 时显示密钥绑定引导；绑定后显示 Shopify 商品表单、AI 解析结果、字段映射、同步状态与解除绑定确认弹窗。
- `产品上架` 页面已加入前端交互按钮：测试连接、保存绑定、解除绑定、重新解析、应用到表单、保存草稿、同步到 Shopify。
- 新增 Shopify 绑定后端接口：读取绑定、测试连接、保存绑定、解除绑定；保存前会请求 Shopify Admin GraphQL API 验证店铺信息，Access Token 加密存入本地数据库。
- `产品上架` 页面绑定区已接入真实后端接口，填写正确的 `xxx.myshopify.com` 和 `Admin API Access Token` 后可测试并保存绑定。
- `产品上架` 绑定成功后显示本地 Shopify 商品编辑布局，左侧新增大模型完整文字输出窗口，可清理多余空格、乱码和无效字符后自动填入商品标题、描述、分类、标签、SKU、价格、SEO 等字段。
- 新增 Shopify 商品同步接口，点击 `同步到 Shopify` 后会通过 Admin GraphQL API 创建 Shopify 商品，并返回可打开的 Shopify 商品后台链接；库存数量后续接入店铺 Location 后再写入。
- `产品上架` 新增测试密钥：`test.myshopify.com` / `shpat_test_buqiqi`，用于测试绑定后页面切换与同步交互；使用该测试密钥时不会调用 Shopify 官方接口。

### 验证

- `npx.cmd tsc --noEmit` 通过。
